export enum ContainerDefaultClass {
  DEFAULT = '.hv-container',
}

export enum ContainerResponsiveClasses {
  SM = '.hv-container_sm',
  MD = '.hv-container_md',
  LG = '.hv-container_lg',
  XL = '.hv-container_xl',
  XXL = '.hv-container_xxl',
}

export enum ContainerFixedClass {
  FIXED = '.hv-container_fixed',
}

export enum ContainerSizeAttribute {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
  XXL = 'xxl',
  FIXED = 'fixed',
}
